In order to build this library follow the next steps

--compile -> ng build <library-name> --configuration=production (optional -> --watch)

-- build -> ng build --project=<library-name>

--package -> cd ./dist/<library-name> and run -> npm pack (will generate a .tgz file)
